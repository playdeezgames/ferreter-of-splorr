namespace FOS.UI
{
    internal static class Commands
    {
        internal static readonly string OK = nameof(OK);
        internal static readonly string EMBARK = nameof(EMBARK);
        internal static readonly string GAME_MENU = nameof(GAME_MENU);
        internal static readonly string RESUME_GAME = nameof(RESUME_GAME);
        internal static readonly string ABANDON_GAME = nameof(ABANDON_GAME);
    }
}
