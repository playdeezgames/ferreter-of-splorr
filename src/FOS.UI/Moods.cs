using System;
using System.Collections.Generic;
using System.Text;

namespace FOS.UI
{
    internal static class Moods
    {
        internal static readonly string TITLE = nameof(TITLE);
        internal static readonly string MENU_HEADER = nameof(MENU_HEADER);
    }
}
